only cache max 5 events

Load dict from file.
If events shown date are > last 5 events:
	add it to dict:
	write dict
	send new event message. 
loop through last 5 dates:
	if date is today:
		send message: event is today
	else if date is 3 days away:
		send message: event is three days away.
